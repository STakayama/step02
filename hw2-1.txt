C,Java,Pythonそれぞれn=2から150までかかった実行時間をプロットしたものが、添付してある画像ファイルである。

それを見ると、n=30あたりまではいずれも近い値だが、C,Javaの実行時間はほぼ0に近く一定に見えるのに対して、Pythonの実行時間は指数関数的に増加していることがわかる。

ただ、Pythonを除いてplotした場合はPythonの場合と同じように指数関数的な増加が起きていることが確認できる。

そのため、ランダウの記号を用いるとすると、多少言語ごとに違いがあるとはいえ、計算量は
いずれもO(nlogn)であると言えると考えられる。
