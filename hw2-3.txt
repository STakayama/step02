与えられた<URL, Webページ>があるかないかを検索する
もしない場合、キャッシュ内で一番古い<URL, Webページ>を捨てて、かわりに与えられた<URL, Webページ>を追加する

->ロケット鉛筆のイメージ。FILOさせたい。追加順を維持できればよいので、古いのを捨てて...とかはできそう。

ハッシュテーブルにインデックスも一緒に保持させるのはどうか。定めた上限をnとして、一番古いのがa、新しいのをa+nとした時に,aを消してa+n+1に追加していく。
それぞれに追加順を保持するリンクのリストのポインタを別にもたせておく。追加順であげていきたい時はそっちのポインタを辿らせる。
ポインタの追加消去のOがちょっと不安だったが、ハッシュテーブルのデータの有無の検索はO(1)で済むはずだし、リストの方は頭と最後をさわるだけなので、計算量は問題ないのでは？

...JavaのLinkedHashMapはアクセス順に要素を並べる機能がある。
Fifo.javaはLinkedHashMapを使って、putされたデータが設定したものより(return size()>...)大きくなった時点で、一番アクセスが古いデータを消し、順繰りに前にデータを動かす。

